# Fix Connect UI + Mouse KVM

Plano executado em 2026-05-17.

- `list_connection_states` + hidratação em `PeersPanel`
- `enable_input_control` / clipboard no `AppLayout`
- `InputEvent::MouseMove` com `screen_x`/`screen_y` para detecção de borda

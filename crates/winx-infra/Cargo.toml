[package]
name        = "winx-infra"
description = "Adapters concretos (impl de ports) do Winx-KVM: Win32, QUIC, mDNS, cpal, etc."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
authors.workspace      = true
license.workspace      = true
repository.workspace   = true

[lints]
workspace = true

[dependencies]
winx-domain      = { workspace = true }
winx-protocol    = { workspace = true }
winx-application = { workspace = true }
tokio            = { workspace = true }
async-trait      = { workspace = true }
serde            = { workspace = true }
toml             = { workspace = true }
thiserror        = { workspace = true }
anyhow           = { workspace = true }
tracing          = { workspace = true }
time             = { workspace = true }

# Deps específicas de adapters serão adicionadas sprint a sprint:
# - mdns-sd       (sprint 2 — discovery)
# - quinn, rustls (sprint 4 — transport)
# - windows       (sprint 5 — input/monitor)
# - cpal, opus    (v0.4   — audio)
# - arboard       (sprint 6 — clipboard)
# - keyring       (sprint 1 — secret store, em breve)
